<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDark</item>
        <item name="colorPrimaryDark">@color/colorDark</item>
        <item name="colorAccent">@color/colorAccentYellow</item>
        <item name="android:colorBackground">@color/colorLessDark</item>
        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="android:textColorPrimaryInverse">@color/colorText</item>
        <item name="android:colorForeground">@color/colorLessDark</item>
        <item name="android:textColorSecondary">@color/colorText</item>
    </style>

</resources>
